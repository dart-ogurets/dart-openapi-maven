/* nullable: {{isNullable}}, vend: {{vendorExtentions}} */
data{{#isNullable}}?{{/isNullable}}.
{{#items}}
  {{#isArray}}
    map((data) { return (({{{dataType}}} data) {
    return {{>_copy}};})(data); }){{#isNullable}}?{{/isNullable}}.toList()
  {{/isArray}}
  {{#isMap}}
    map((data) { ((data) { return {{>_copy}};})(data); }){{#isNullable}}?{{/isNullable}}.toList()
  {{/isMap}}
  {{^items}}
    {{^isModel}}
      toList()
    {{/isModel}}
    {{#isModel}}
      map((data) => {{>_copy}}){{#isNullable}}?{{/isNullable}}.toList()
    {{/isModel}}
  {{/items}}
{{/items}}
