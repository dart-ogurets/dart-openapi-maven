{{#items}}
  data{{#vendorExtensions.x-list-null}}?{{/vendorExtensions.x-list-null}}.
  {{#isArray}}
    map((data) { return (({{{dataType}}} data) {
    return {{>_copy}};})(data); }){{#vendorExtensions.x-list-null}}?{{/vendorExtensions.x-list-null}}.toList()
  {{/isArray}}
  {{#isMap}}
    map((data) { ((data) { return {{>_copy}};})(data); }).toList()
  {{/isMap}}
  {{^items}}
    {{^isModel}}
      toList()
    {{/isModel}}
    {{#isModel}}
      map((data) => {{>_copy}}).toList()
    {{/isModel}}
  {{/items}}
{{/items}}
